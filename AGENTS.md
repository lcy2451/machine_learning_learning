# 项目协作规范

## 项目定位

这是个人机器学习学习与练习仓库。
优先保证代码容易理解，不要过度封装或提前设计复杂架构。

## 交流与代码检查

- 使用中文回答，解释要适合机器学习初学者。
- 用户说“看看代码”时，先读取最新文件，再进行检查。
- 代码检查只给出意见，不直接修改，除非用户要求修改。
- 按当前明确支持的功能范围检查，不反复讨论已接受的限制。
- 区分实际错误和可选改进。

## 代码与环境

- 使用 Anaconda 的 `machine_learning` 环境。
- 不自行创建环境或安装依赖。
- 保留有助于学习的中文注释。
- 文件名简短，并能区分算法、框架和输入特征数量。
- 数据集统一放在根目录 `data` 文件夹。

## 文档

- README 用于介绍现有内容，不写学习计划或待办清单。
- 根据实际代码描述功能，不将未实现内容写成已完成。

## Git

- 提交说明使用中文。
- 仅在用户要求时提交或推送；用户要求提交时，同时完成 commit 和 push，无需再单独确认推送。
- 不提交 `.idea` 等本地 IDE 配置。
